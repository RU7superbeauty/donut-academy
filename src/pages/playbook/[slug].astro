---
const workflows = ['funding-rate-arb', 'whale-monitor', 'mm-detection'];
export function getStaticPaths() {
  return ['funding-rate-arb', 'whale-monitor', 'mm-detection'].map(slug => ({ params: { slug } }));
}
const { slug } = Astro.params;
return Astro.redirect(`/en/playbook/${slug}/`);
---
